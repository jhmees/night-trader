# Secrets — never committed (design §11.1)
.env
.env.*
!.env.example

# Data lake — parquet lives locally, not in git (design §11.2)
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/
uv.lock
